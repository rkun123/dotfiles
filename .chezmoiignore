{{/* リポジトリ管理用ファイル (ホームには適用しない) */}}
README.md
AGENTS.md
.cursor
tools
polybar

{{- if not (lookPath "nvim") }}
.config/nvim
{{- end }}

{{- if not (lookPath "wezterm") }}
.config/wezterm
{{- end }}

{{- if not (lookPath "tmux") }}
.tmux.conf
{{- end }}

{{- if not (lookPath "starship") }}
.config/starship.toml
{{- end }}

{{- if not (lookPath "vim") }}
.vimrc
{{- end }}

{{- if not (lookPath "hyper") }}
.hyper.js
{{- end }}

{{- if not (lookPath "zsh") }}
.zshrc
.zprofile
{{- end }}

{{/* 以下は X11 前提の Linux デスクトップ専用ツール */}}

{{- if or (ne .chezmoi.os "linux") (not (lookPath "i3")) }}
.config/i3
{{- end }}

{{- if or (ne .chezmoi.os "linux") (not (lookPath "polybar")) }}
.config/polybar
{{- end }}

{{- if or (ne .chezmoi.os "linux") (not (or (lookPath "compton") (lookPath "picom"))) }}
compton.conf
{{- end }}

{{/* Xorg や xinit、startx など X11 関連コマンドのどれも存在しない場合 */}}
{{- if or (ne .chezmoi.os "linux") (not (or (lookPath "Xorg") (lookPath "xinit") (lookPath "startx"))) }}
.xinitrc
{{- end }}
